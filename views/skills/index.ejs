<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Allie's Express Dev Skills</title>
</head>
<body>
    <h1>Allie's Dev Skills</h1>
    <ul>
        <% skills.forEach(function(skill){ %>
            <li>
                <a href="/skills/<%=skill.id%>">
                    <%= skill.skill %>
                </a>
                 - 
                <%= skill.comfortLevel %>
                 - 
                <%= skill.encountered ? 'encountered!' : 'have not seen this yet!' %> 
            </li> 
        <% }); %> 
    </ul>
</body>
</html>