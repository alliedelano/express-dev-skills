<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Allie's Express Dev Skills</title>
</head>
<body>
    <h1>Skill #<%= skill.id %></h1>
    <h3 style="background-color:<%= skill.comfortLevel %>;"><%= skill.skill %></h3>
    <h3><%= skill.encountered ? 'encountered!' : 'have not seen this yet!' %> </h3>
    <form action="/skills/<%= skill.id %>?_method=DELETE" class="delete-form" method="POST">
        <button type="submit">delete!</button>
    </form>
</body>
</html>